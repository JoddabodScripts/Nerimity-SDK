{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"nerimity-sdk","text":"<p>A Python library for building bots on Nerimity.</p> <pre><code>pip install nerimity-sdk\n</code></pre>"},{"location":"#how-it-works","title":"How it works","text":"<p>You create a <code>Bot</code>, attach handlers with decorators, then call <code>bot.run()</code>. The bot connects over a WebSocket, receives events, and dispatches them to your handlers.</p> <pre><code>Nerimity \u2192 WebSocket \u2192 GatewayClient \u2192 EventEmitter \u2192 your handlers\n</code></pre> <p>Everything your handlers need is in the <code>ctx</code> object passed to them.</p>"},{"location":"#quickstart","title":"Quickstart","text":"<p>Get a token: nerimity.com/app/settings/developer/applications \u2192 create app \u2192 add Bot \u2192 copy token.</p> <pre><code>nerimity create my-bot\ncd my-bot &amp;&amp; cp .env.example .env   # open .env and paste token\npython bot.py\n</code></pre> <p>Or manually:</p> <pre><code>import os\nfrom dotenv import load_dotenv\nfrom nerimity_sdk import Bot\n\nload_dotenv()\nbot = Bot(token=os.environ[\"NERIMITY_TOKEN\"], prefix=\"!\") # or make a .env file with this in it NERIMITY_TOKEN=your_token\n\n@bot.on(\"ready\")\nasync def on_ready(me):\n    print(f\"Logged in as {me.username}#{me.tag}\")\n\n@bot.command(\"ping\", description=\"Replies with Pong!\")\nasync def ping(ctx):\n    await ctx.reply(\"Pong!\")\n\nbot.run()\n</code></pre>"},{"location":"#whats-available","title":"What's available","text":"Feature How to use Event listeners <code>@bot.on(\"message:created\")</code> Prefix + slash commands <code>@bot.command(\"ping\")</code> \u2014 works as <code>!ping</code> and <code>/ping</code> Prefix-only commands <code>@bot.command_private(\"debug\")</code> Argument converters <code>args=[Int, MemberConverter]</code> Confirmation prompts <code>await ctx.confirm(\"Sure?\")</code> Multi-step conversations <code>await ctx.ask(\"Your name?\")</code> DMs <code>await ctx.author.send(bot.rest, \"Hi!\")</code> Edit messages <code>await ctx.edit(msg.id, \"updated!\")</code> File uploads <code>await ctx.reply_file(\"image.png\")</code> Paginator <code>await Paginator(pages).send(ctx)</code> Mention helpers <code>mention(user_id)</code> / <code>ctx.mentions</code> Webhooks <code>await Webhook(token).send(\"Hello!\")</code> Persistent storage <code>JsonStore</code> / <code>SqliteStore</code> / <code>RedisStore</code> Scheduled tasks <code>@bot.cron(\"0 9 * * *\")</code> Event waiting (typed) <code>await bot.wait_for(\"member_joined\", ...)</code> Plugins (hot-reload) <code>await bot.plugins.load(MyPlugin())</code> Contrib plugins <code>pip install nerimity-sdk-contrib</code> Error handlers <code>@bot.on_command_error</code> Cooldown feedback automatic \"try again in Xs\" message Stale cache detection <code>user.stale == True</code> after reconnect Static analysis <code>nerimity lint</code> Debug mode <code>Bot(debug=True)</code> Auto-reload on save <code>Bot(watch=True)</code> <p>See the Getting Started guide or the Example Bot for a full working example.</p>"},{"location":"example/","title":"Example Bot","text":"<p>A complete bot demonstrating every major feature. Source: <code>example_bot/bot.py</code></p>"},{"location":"example/#setup","title":"Setup","text":"<pre><code>pip install \"nerimity-sdk[cron]\"\nexport NERIMITY_TOKEN=your_token_here\nexport ANNOUNCE_CHANNEL=your_channel_id   # optional, for cron demo\npython example_bot/bot.py\n</code></pre>"},{"location":"example/#what-it-demonstrates","title":"What it demonstrates","text":""},{"location":"example/#prefix-commands-with-converters","title":"Prefix commands with converters","text":"<pre><code>from nerimity_sdk import Int, MemberConverter\n\n@bot.command(\"add\", args=[Int, Int])\nasync def add(ctx):\n    a, b = ctx.args   # already ints\n    await ctx.reply(f\"{a} + {b} = {a + b}\")\n\n@bot.command(\"kick\", args=[MemberConverter], guild_only=True)\nasync def kick(ctx):\n    member = ctx.args[0]\n    if not await ctx.confirm(f\"Kick {member.user.username}?\"):\n        return await ctx.reply(\"Cancelled.\")\n    await ctx.rest.kick_member(ctx.server_id, member.user.id)\n    await ctx.reply(f\"Kicked {mention(member.user.id)}\")\n</code></pre>"},{"location":"example/#slash-command","title":"Slash command","text":"<pre><code>@bot.slash(\"info\", description=\"Show bot info\")\nasync def slash_info(sctx):\n    from nerimity_sdk import __version__\n    await sctx.reply(f\"nerimity-sdk v{__version__}\")\n</code></pre>"},{"location":"example/#poll-with-reactions","title":"Poll with reactions","text":"<pre><code>@bot.command(\"poll\", description=\"Start a yes/no poll\")\nasync def poll(ctx):\n    question = \" \".join(ctx.args) if ctx.args else \"Do you agree?\"\n    msg = await ctx.reply(f\"\ud83d\udcca **{question}**\\n\ud83d\udc4d Yes  |  \ud83d\udc4e No\")\n    await ctx.rest.add_reaction(msg.channel_id, msg.id, \"\ud83d\udc4d\")\n    await ctx.rest.add_reaction(msg.channel_id, msg.id, \"\ud83d\udc4e\")\n</code></pre>"},{"location":"example/#paginator","title":"Paginator","text":"<pre><code>@bot.command(\"help\")\nasync def help_cmd(ctx):\n    pages = bot.router.help_text().split(\"\\n\\n\")\n    await Paginator(pages).send(ctx)\n</code></pre>"},{"location":"example/#scheduled-task","title":"Scheduled task","text":"<pre><code>@bot.cron(\"0 9 * * 1\")   # Every Monday 09:00 UTC\nasync def weekly():\n    await bot.rest.create_message(ANNOUNCE_CHANNEL, \"Good morning!\")\n</code></pre>"},{"location":"example/#persistent-storage","title":"Persistent storage","text":"<pre><code>@bot.command(\"setprefix\")\nasync def setprefix(ctx):\n    await bot.store.set(f\"guild:{ctx.server_id}:prefix\", ctx.args[0])\n    await ctx.reply(f\"Prefix set to `{ctx.args[0]}`\")\n</code></pre>"},{"location":"example/#global-error-handlers","title":"Global error handlers","text":"<pre><code>@bot.on_command_error\nasync def on_error(ctx, error):\n    await ctx.reply(f\"\u274c {error}\")\n\n@bot.on_slash_error\nasync def on_slash_error(sctx, error):\n    await sctx.reply(f\"\u274c {error}\")\n\n@bot.on_button_error\nasync def on_button_error(bctx, error):\n    await bctx.reply(f\"\u274c {error}\")\n</code></pre>"},{"location":"example/#plugin","title":"Plugin","text":"<pre><code>from nerimity_sdk import PluginBase, listener\n\nclass WelcomePlugin(PluginBase):\n    name = \"welcome\"\n\n    @listener(\"server:member_joined\")\n    async def on_join(self, event):\n        await self.bot.rest.create_message(\n            channel_id, f\"Welcome {mention(event.member.user.id)}!\"\n        )\n\nasync def main():\n    await bot.plugins.load(WelcomePlugin())\n    bot.run()\n</code></pre>"},{"location":"example/#full-source","title":"Full source","text":"<p>See <code>example_bot/bot.py</code> for the complete runnable file.</p>"},{"location":"plugins/","title":"Plugin Marketplace","text":"<p>Community plugins for nerimity-sdk. To add yours, open a PR to the docs repo.</p>"},{"location":"plugins/#official-contrib-plugins","title":"Official contrib plugins","text":"<pre><code>pip install nerimity-sdk-contrib\n</code></pre> <pre><code>from nerimity_sdk_contrib import WelcomePlugin, AutoModPlugin, StarboardPlugin, LoggingPlugin\n\nawait bot.plugins.load(WelcomePlugin(channel_id=\"123\"))\nawait bot.plugins.load(AutoModPlugin(blocked=[\"badword\"], log_channel_id=\"456\"))\nawait bot.plugins.load(StarboardPlugin(channel_id=\"789\", threshold=3))\nawait bot.plugins.load(LoggingPlugin(channel_id=\"000\"))\n</code></pre>"},{"location":"plugins/#available-plugins","title":"Available plugins","text":"Plugin Description Options <code>WelcomePlugin</code> Greets new members <code>channel_id</code>, <code>message</code> (supports <code>{mention}</code>, <code>{username}</code>, <code>{tag}</code>) <code>AutoModPlugin</code> Deletes messages matching a word/regex list <code>blocked</code>, <code>log_channel_id</code> <code>StarboardPlugin</code> Reposts highly-reacted messages to a starboard channel <code>channel_id</code>, <code>emoji</code>, <code>threshold</code> <code>LoggingPlugin</code> Logs joins, leaves, deletes, and edits <code>channel_id</code> <code>RoleMenuPlugin</code> React to a message to get a role, unreact to remove it <code>message_id</code>, <code>roles</code> (dict of emoji \u2192 role_id) <code>PollPlugin</code> Timed reaction poll with automatic result tallying loaded once, then <code>await plugin.create(ctx, question, options, duration)</code>"},{"location":"plugins/#community-plugins","title":"Community plugins","text":"<p>None listed yet \u2014 be the first!</p> <p>To list your plugin here, open a PR adding a row to this table:</p> Plugin Author Description Install your-plugin @you What it does <code>pip install your-plugin</code>"},{"location":"plugins/#writing-a-plugin","title":"Writing a plugin","text":"<pre><code>from nerimity_sdk import PluginBase, listener\n\nclass MyPlugin(PluginBase):\n    name = \"my_plugin\"\n\n    @listener(\"message:created\")\n    async def on_message(self, event):\n        if \"hello\" in event.message.content.lower():\n            await self.bot.rest.create_message(event.message.channel_id, \"Hello!\")\n\nasync def setup(bot):\n    await bot.plugins.load(MyPlugin())\n</code></pre> <p>See the Plugins API reference for the full guide.</p>"},{"location":"api/bot/","title":"Bot","text":"<p>The central class. Import and instantiate once per process.</p> <pre><code>from nerimity_sdk import Bot\nbot = Bot(token=\"YOUR_TOKEN\", prefix=\"!\")\n</code></pre>"},{"location":"api/bot/#decorators","title":"Decorators","text":""},{"location":"api/bot/#botonevent","title":"<code>@bot.on(event)</code>","text":"<p>Register an async listener for a gateway event. Receives a typed payload object.</p> <pre><code>@bot.on(\"message:created\")\nasync def handler(event: MessageCreatedEvent): ...\n\n@bot.on(\"ready\")\nasync def on_ready(me: User): ...\n\n@bot.on(\"*\")          # wildcard \u2014 fires for every event\nasync def log_all(data): ...\n</code></pre>"},{"location":"api/bot/#botonceevent","title":"<code>@bot.once(event)</code>","text":"<p>Same as <code>on()</code> but fires only once.</p>"},{"location":"api/bot/#botcommandname-kwargs","title":"<code>@bot.command(name, **kwargs)</code>","text":"<p>Register a prefix command. See Commands.</p>"},{"location":"api/bot/#botslashname-kwargs","title":"<code>@bot.slash(name, **kwargs)</code>","text":"<p>Register a slash command. See Slash Commands.</p>"},{"location":"api/bot/#botbuttonpattern-ttlnone","title":"<code>@bot.button(pattern, ttl=None)</code>","text":"<p>Register a button handler. See Buttons.</p>"},{"location":"api/bot/#botcronexpr","title":"<code>@bot.cron(expr)</code>","text":"<p>Register a scheduled task. See Scheduler.</p>"},{"location":"api/bot/#boton_command_error","title":"<code>@bot.on_command_error</code>","text":"<pre><code>@bot.on_command_error\nasync def handler(ctx: Context, error: Exception): ...\n</code></pre>"},{"location":"api/bot/#boton_slash_error","title":"<code>@bot.on_slash_error</code>","text":"<pre><code>@bot.on_slash_error\nasync def handler(sctx: SlashContext, error: Exception): ...\n</code></pre>"},{"location":"api/bot/#boton_button_error","title":"<code>@bot.on_button_error</code>","text":"<pre><code>@bot.on_button_error\nasync def handler(bctx: ButtonContext, error: Exception): ...\n</code></pre>"},{"location":"api/bot/#properties","title":"Properties","text":"Property Type Description <code>bot.rest</code> <code>RESTClient</code> Direct HTTP API access <code>bot.cache</code> <code>Cache</code> In-memory object cache <code>bot.store</code> <code>Store</code> Persistent key-value storage <code>bot.plugins</code> <code>PluginManager</code> Load/unload/reload plugins <code>bot.scheduler</code> <code>Scheduler</code> Cron job manager <code>bot.prefix_resolver</code> <code>PrefixResolver</code> Per-guild prefix config <code>bot.router</code> <code>CommandRouter</code> Prefix command router <code>bot.slash_router</code> <code>SlashRouter</code> Slash command router <code>bot.button_router</code> <code>ButtonRouter</code> Button handler router"},{"location":"api/bot/#methods","title":"Methods","text":""},{"location":"api/bot/#botrun","title":"<code>bot.run()</code>","text":"<p>Blocking entry point. Handles <code>SIGINT</code>/<code>SIGTERM</code> gracefully.</p>"},{"location":"api/bot/#await-botstart","title":"<code>await bot.start()</code>","text":"<p>Async version \u2014 use if you need to run alongside other async code.</p>"},{"location":"api/bot/#await-botclose","title":"<code>await bot.close()</code>","text":"<p>Flush queues, stop scheduler, disconnect gateway, close HTTP session.</p>"},{"location":"api/bot/#botfrom_shardtoken-shard_id-shard_count-kwargs","title":"<code>Bot.from_shard(token, shard_id, shard_count, **kwargs)</code>","text":"<p>Create a bot instance for a specific shard (architecture is ready; Nerimity sharding is a stub).</p>"},{"location":"api/buttons/","title":"Buttons","text":""},{"location":"api/buttons/#building-buttons","title":"Building buttons","text":"<pre><code>from nerimity_sdk import Button\n\nmsg = await ctx.reply(\n    \"Are you sure?\",\n    buttons=[\n        Button(id=\"confirm:delete:123\", label=\"\u2705 Yes, delete\"),\n        Button(id=\"cancel:delete:123\",  label=\"\u274c Cancel\", alert=True),\n    ]\n)\n</code></pre> <p><code>alert=True</code> renders the button in red.</p>"},{"location":"api/buttons/#handling-clicks","title":"Handling clicks","text":"<pre><code>@bot.button(\"confirm:{action}:{target}\")\nasync def on_confirm(bctx):\n    action = bctx.params[\"action\"]   # \"delete\"\n    target = bctx.params[\"target\"]   # \"123\"\n    # do the thing...\n    await bctx.popup(\"Done!\", f\"{action} on {target} completed.\")\n\n@bot.button(\"cancel:{action}:{target}\")\nasync def on_cancel(bctx):\n    await bctx.popup(\"Cancelled\", \"Nothing was changed.\")\n</code></pre> <p><code>bctx.popup(title, content)</code> shows a modal dialog to the user who clicked \u2014 it doesn't post a message in the channel.</p> <p><code>bctx.reply(content)</code> posts a message in the channel instead.</p>"},{"location":"api/buttons/#ttl-auto-expiry","title":"TTL (auto-expiry)","text":"<pre><code>@bot.button(f\"confirm:{msg.id}\", ttl=300)   # expires after 5 minutes\nasync def on_confirm(bctx):\n    ...\n</code></pre> <p>After the TTL expires the handler is removed and clicks are silently ignored.</p>"},{"location":"api/buttons/#registering-handlers","title":"Registering handlers","text":"<p>Use <code>{name}</code> segments in patterns to capture dynamic parts:</p> <pre><code>@bot.button(\"confirm:{action}:{target}\")\nasync def on_confirm(bctx):\n    action = bctx.params[\"action\"]   # e.g. \"delete\"\n    target = bctx.params[\"target\"]   # e.g. \"123\"\n    await bctx.reply(f\"Confirmed: {action} on {target}\")\n</code></pre> <p>Wildcard patterns also work:</p> <pre><code>@bot.button(\"poll:yes:*\")\nasync def on_yes(bctx): ...\n</code></pre>"},{"location":"api/buttons/#ttl-time-to-live","title":"TTL (time-to-live)","text":"<p>Registrations expire after <code>ttl</code> seconds. Old buttons stop responding cleanly:</p> <pre><code>@bot.button(\"vote:*\", ttl=300)   # expires after 5 minutes\nasync def on_vote(bctx): ...\n</code></pre>"},{"location":"api/buttons/#buttoncontext","title":"ButtonContext","text":"Property Type Description <code>bctx.button_id</code> <code>str</code> Full button ID <code>bctx.message_id</code> <code>str</code> Message containing the button <code>bctx.channel_id</code> <code>str</code> Channel ID <code>bctx.server_id</code> <code>str \\| None</code> Server ID <code>bctx.user_id</code> <code>str</code> User who clicked <code>bctx.user</code> <code>User \\| None</code> Resolved from cache <code>bctx.params</code> <code>dict[str, str]</code> Captured pattern segments"},{"location":"api/buttons/#await-bctxreplycontent","title":"<code>await bctx.reply(content)</code>","text":"<p>Send a message to the channel.</p>"},{"location":"api/buttons/#await-bctxupdate_messagecontent","title":"<code>await bctx.update_message(content)</code>","text":"<p>Edit the message that contains the button.</p>"},{"location":"api/buttons/#await-bctxdefer","title":"<code>await bctx.defer()</code>","text":"<p>Acknowledge the click silently (no-op placeholder for API parity).</p>"},{"location":"api/buttons/#error-handling","title":"Error handling","text":"<pre><code>@bot.on_button_error\nasync def on_button_error(bctx, error):\n    await bctx.reply(f\"\u274c {error}\")\n</code></pre>"},{"location":"api/cache/","title":"Cache","text":"<p>In-memory LRU/TTL cache for all Nerimity objects.</p>"},{"location":"api/cache/#accessing-cached-objects","title":"Accessing cached objects","text":"<pre><code>server  = bot.cache.servers.get(\"server_id\")\nchannel = bot.cache.channels.get(\"channel_id\")\nuser    = bot.cache.users.get(\"user_id\")\nmember  = bot.cache.members.get(\"server_id:user_id\")\nmessage = bot.cache.messages.get(\"message_id\")\n</code></pre>"},{"location":"api/cache/#partial-merge","title":"Partial merge","text":"<p>Gateway events often send partial objects. The cache merges them intelligently \u2014 a <code>{id, username}</code> update won't wipe the avatar field.</p>"},{"location":"api/cache/#configuration","title":"Configuration","text":"<pre><code>bot = Bot(\n    token=\"...\",\n    cache_size=1000,   # max objects per type\n    cache_ttl=3600,    # expire after 1 hour (0 = never)\n    cache_invalidate_on_disconnect=True,  # wipe on reconnect\n)\n</code></pre>"},{"location":"api/cache/#redis-adapter","title":"Redis adapter","text":"<p>For multi-process bots, use the Redis adapter:</p> <pre><code>import redis.asyncio as aioredis\nfrom nerimity_sdk.cache.redis_adapter import RedisCache\n\nr = aioredis.from_url(\"redis://localhost\")\ncache = RedisCache(r, prefix=\"mybot:\", ttl=3600)\n</code></pre>"},{"location":"api/commands/","title":"Commands","text":"<p>Commands are functions that run when a user types <code>!ping</code> or <code>/ping</code> in chat.</p> <p><code>@bot.command</code> does both at once \u2014 it registers the command with Nerimity's <code>/</code> slash menu and handles the prefix version. You only write the handler once.</p> <pre><code>@bot.command(\"ping\", description=\"Check if the bot is alive\")\nasync def ping(ctx):\n    await ctx.reply(\"Pong! \ud83c\udfd3\")\n</code></pre> <p>Users can now trigger this with <code>!ping</code> or <code>/ping</code>. That's it.</p> <p>If you want a command that only works with the prefix and never appears in the <code>/</code> menu:</p> <pre><code>@bot.command_private(\"debug\", description=\"Internal debug info\")\nasync def debug(ctx):\n    await ctx.reply(\"secret stuff\")\n</code></pre> <p><code>@bot.slash</code> and <code>@bot.slash_private</code> are just aliases:</p> <pre><code>@bot.slash(\"ping\")           # identical to @bot.command(\"ping\")\n@bot.slash_private(\"debug\")  # identical to @bot.command_private(\"debug\")\n</code></pre>"},{"location":"api/commands/#reading-arguments","title":"Reading arguments","text":"<p>When a user types <code>!add 5 10</code>, the words after the command name are in <code>ctx.args</code>:</p> <pre><code>@bot.command(\"add\")\nasync def add(ctx):\n    # ctx.args = [\"5\", \"10\"]  \u2190 raw strings\n    a = int(ctx.args[0])\n    b = int(ctx.args[1])\n    await ctx.reply(f\"{a + b}\")\n</code></pre> <p>But doing <code>int()</code> yourself is annoying and gives ugly errors. Use converters instead:</p> <pre><code>from nerimity_sdk import Int\n\n@bot.command(\"add\", args=[Int, Int])\nasync def add(ctx):\n    a, b = ctx.args   # already ints \u2014 if the user types garbage, they get a friendly error\n    await ctx.reply(f\"{a + b}\")\n</code></pre>"},{"location":"api/commands/#available-converters","title":"Available converters","text":"Converter What it accepts What you get <code>Int</code> <code>42</code>, <code>-5</code> <code>int</code> <code>MemberConverter</code> <code>[@:id]</code>, user ID, username <code>Member</code> object <code>UserConverter</code> <code>[@:id]</code>, user ID <code>User</code> object <code>ChannelConverter</code> channel ID <code>Channel</code> object <p>If conversion fails, the bot automatically replies with a friendly error and stops \u2014 your function never runs.</p>"},{"location":"api/commands/#flags","title":"Flags","text":"<p>Users can pass named options with <code>--</code>:</p> <pre><code>!ban @user --reason=\"spamming\" --silent\n</code></pre> <pre><code>@bot.command(\"ban\")\nasync def ban(ctx):\n    reason = ctx.flags.get(\"reason\", \"No reason given\")\n    silent = ctx.flags.get(\"silent\", False)\n    await ctx.reply(f\"Banned. Reason: {reason}\")\n</code></pre>"},{"location":"api/commands/#quoted-strings","title":"Quoted strings","text":"<p>Quoted arguments are kept together:</p> <pre><code>!echo \"hello world\"   \u2192   ctx.args = [\"hello world\"]\n!echo hello world     \u2192   ctx.args = [\"hello\", \"world\"]\n</code></pre>"},{"location":"api/commands/#useful-options","title":"Useful options","text":"<pre><code>@bot.command(\n    \"kick\",\n    description=\"Kick a member\",   # shown in !help\n    usage=\"&lt;@member&gt; [reason]\",     # shown in !help\n    category=\"Moderation\",          # groups commands in !help\n    aliases=[\"k\"],                  # !k works too\n    guild_only=True,                # DMs get a friendly error\n    cooldown=5.0,                   # 5 second per-user cooldown\n    args=[MemberConverter],         # auto-convert first arg to Member\n)\nasync def kick(ctx):\n    member = ctx.args[0]\n    await ctx.rest.kick_member(ctx.server_id, member.user.id)\n    await ctx.reply(f\"Kicked {member.user.username}.\")\n</code></pre>"},{"location":"api/commands/#confirmation-prompts","title":"Confirmation prompts","text":"<p>For destructive commands, ask the user to confirm before doing anything:</p> <pre><code>@bot.command(\"clear\", args=[Int])\nasync def clear(ctx):\n    count = ctx.args[0]\n\n    confirmed = await ctx.confirm(f\"Delete {count} messages?\")\n    if not confirmed:\n        return await ctx.reply(\"Cancelled.\")\n\n    msgs = await ctx.fetch_messages(limit=count)\n    await ctx.rest.bulk_delete_messages(ctx.channel_id, [m.id for m in msgs])\n    await ctx.reply(f\"Deleted {len(msgs)} messages.\")\n</code></pre> <p><code>ctx.confirm()</code> sends the prompt, waits for the user to reply yes or no, and returns <code>True</code>, <code>False</code>, or <code>None</code> (timeout).</p>"},{"location":"api/commands/#multi-step-conversations","title":"Multi-step conversations","text":"<p>Ask follow-up questions with <code>ctx.ask()</code>:</p> <pre><code>@bot.command(\"setup\")\nasync def setup(ctx):\n    name = await ctx.ask(\"What should I call you?\", timeout=30)\n    if name is None:\n        return await ctx.reply(\"Timed out.\")\n\n    await bot.store.set(f\"user:{ctx.author.id}:name\", name.content)\n    await ctx.reply(f\"Got it, {name.content}!\")\n</code></pre>"},{"location":"api/commands/#permission-checks","title":"Permission checks","text":"<p>Only run a command if the user has the right permissions:</p> <pre><code>from nerimity_sdk import Permissions\n\n@bot.command(\"ban\", required_user_perms=[Permissions.BAN_MEMBERS])\nasync def ban(ctx):\n    ...  # only runs if the user has BAN_MEMBERS\n         # otherwise they get \"You need the `ban_members` permission.\"\n</code></pre>"},{"location":"api/commands/#middleware","title":"Middleware","text":"<p>Middleware runs before your command. Return <code>False</code> to stop the command:</p> <pre><code>async def admins_only(ctx, next):\n    if ctx.author.id not in ADMIN_IDS:\n        await ctx.reply(\"Admins only.\")\n        return False   # \u2190 stops here, command never runs\n    await next(ctx)    # \u2190 continue to the command\n\n# Apply to one command:\n@bot.command(\"secret\", middleware=[admins_only])\nasync def secret(ctx): ...\n\n# Apply to all commands:\nbot.router.use(admins_only)\n</code></pre>"},{"location":"api/commands/#auto-generated-help","title":"Auto-generated help","text":"<p><code>bot.router.help_text()</code> builds a help message from your command metadata automatically:</p> <pre><code>@bot.command(\"help\")\nasync def help_cmd(ctx):\n    await ctx.reply(bot.router.help_text())\n\n# Filter to one category:\n    await ctx.reply(bot.router.help_text(category=\"Moderation\"))\n</code></pre> <p>For long help menus, use the paginator:</p> <pre><code>from nerimity_sdk import Paginator\n\n@bot.command(\"help\")\nasync def help_cmd(ctx):\n    pages = bot.router.help_text().split(\"\\n\\n\")\n    await Paginator(pages).send(ctx)\n</code></pre>"},{"location":"api/commands/#error-handling","title":"Error handling","text":"<p>Without this, errors are logged silently. With it, users see a friendly message:</p> <pre><code>@bot.on_command_error\nasync def on_error(ctx, error):\n    await ctx.reply(f\"\u274c Something went wrong: {error}\")\n</code></pre>"},{"location":"api/context/","title":"Context","text":"<p>Passed to every prefix command handler. Wraps the triggering message with convenience helpers.</p>"},{"location":"api/context/#properties","title":"Properties","text":"Property Type Description <code>ctx.message</code> <code>Message</code> The raw message object <code>ctx.author</code> <code>User</code> Message author <code>ctx.channel_id</code> <code>str</code> Channel snowflake ID <code>ctx.server_id</code> <code>str \\| None</code> Server ID (None in DMs) <code>ctx.server</code> <code>Server \\| None</code> Server from cache <code>ctx.channel</code> <code>Channel \\| None</code> Channel from cache <code>ctx.member</code> <code>Member \\| None</code> Author's server member from cache <code>ctx.args</code> <code>list</code> Parsed positional arguments (converted if <code>args=</code> set) <code>ctx.flags</code> <code>dict</code> Named flags (<code>--flag=value</code>) <code>ctx.mentions</code> <code>list[User]</code> Resolved <code>[@:id]</code> mentions from message content"},{"location":"api/context/#methods","title":"Methods","text":""},{"location":"api/context/#await-ctxreplycontent","title":"<code>await ctx.reply(content)</code>","text":"<p>Send a message to the same channel. Returns <code>Message</code>.</p>"},{"location":"api/context/#await-ctxreactemoji-emoji_idnone-giffalse-webpfalse","title":"<code>await ctx.react(emoji, emoji_id=None, gif=False, webp=False)</code>","text":"<p>Add a reaction to the triggering message.</p> <pre><code>await ctx.react(\"\ud83d\udc4d\")                          # unicode\nawait ctx.react(\"wave\", emoji_id=\"123456\")    # custom emoji\n</code></pre>"},{"location":"api/context/#await-ctxunreactemoji-emoji_idnone","title":"<code>await ctx.unreact(emoji, emoji_id=None)</code>","text":"<p>Remove a reaction.</p>"},{"location":"api/context/#await-ctxaskprompt-timeout300-checknone","title":"<code>await ctx.ask(prompt, timeout=30.0, check=None)</code>","text":"<p>Send a prompt and wait for the author's next message in the same channel. Returns <code>Message</code> or <code>None</code> on timeout.</p> <pre><code>name = await ctx.ask(\"What's your name?\", timeout=30)\nif name:\n    await ctx.reply(f\"Hi {name.content}!\")\n</code></pre>"},{"location":"api/context/#await-ctxconfirmprompt-timeout300","title":"<code>await ctx.confirm(prompt, timeout=30.0)</code>","text":"<p>Ask a yes/no question. Returns <code>True</code>, <code>False</code>, or <code>None</code> on timeout.</p> <pre><code>if not await ctx.confirm(\"Are you sure?\"):\n    return await ctx.reply(\"Cancelled.\")\n</code></pre> <p>Accepts: <code>yes</code>, <code>y</code>, <code>yeah</code>, <code>yep</code>, <code>confirm</code>, <code>ok</code> \u2192 <code>True</code> Accepts: <code>no</code>, <code>n</code>, <code>nope</code>, <code>cancel</code>, <code>abort</code> \u2192 <code>False</code></p>"},{"location":"api/context/#await-ctxfetch_messageslimit50-beforenone-afternone","title":"<code>await ctx.fetch_messages(limit=50, before=None, after=None)</code>","text":"<p>Fetch messages from the channel. Returns <code>list[Message]</code>.</p>"},{"location":"api/context/#await-ctxfetch_memberuser_id","title":"<code>await ctx.fetch_member(user_id)</code>","text":"<p>Look up a member from cache. Returns <code>Member | None</code>.</p>"},{"location":"api/context/#await-ctxsend_typing","title":"<code>await ctx.send_typing()</code>","text":"<p>Send a typing indicator to the channel.</p>"},{"location":"api/events/","title":"Events","text":""},{"location":"api/events/#listening","title":"Listening","text":"<pre><code>@bot.on(\"message:created\")\nasync def handler(event: MessageCreatedEvent): ...\n\n@bot.once(\"ready\")\nasync def on_first_ready(me: User): ...\n\n@bot.on(\"*\")   # wildcard\nasync def log_all(data): ...\n</code></pre>"},{"location":"api/events/#typed-payload-classes","title":"Typed payload classes","text":"Event Payload class Key fields <code>user:authenticated</code> <code>ReadyEvent</code> <code>.user</code>, <code>.servers</code>, <code>.channels</code>, <code>.members</code>, <code>.roles</code> <code>message:created</code> <code>MessageCreatedEvent</code> <code>.message</code>, <code>.server_id</code>, <code>.socket_id</code> <code>message:updated</code> <code>MessageUpdatedEvent</code> <code>.channel_id</code>, <code>.message_id</code>, <code>.updated</code> <code>message:deleted</code> <code>MessageDeletedEvent</code> <code>.channel_id</code>, <code>.message_id</code> <code>message:reaction_added</code> <code>ReactionAddedEvent</code> <code>.message_id</code>, <code>.name</code>, <code>.count</code>, <code>.reacted_by_user_id</code> <code>message:reaction_removed</code> <code>ReactionRemovedEvent</code> <code>.message_id</code>, <code>.name</code>, <code>.count</code> <code>server:member_joined</code> <code>MemberJoinedEvent</code> <code>.member</code>, <code>.server_id</code> <code>server:member_left</code> <code>MemberLeftEvent</code> <code>.server_id</code>, <code>.user_id</code> <code>server:updated</code> <code>ServerUpdatedEvent</code> <code>.server_id</code>, <code>.updated</code> <code>server:channel_created</code> <code>ChannelCreatedEvent</code> <code>.channel</code> <code>server:channel_updated</code> <code>ChannelUpdatedEvent</code> <code>.channel_id</code>, <code>.updated</code> <code>server:channel_deleted</code> <code>ChannelDeletedEvent</code> <code>.channel_id</code> <code>server:role_created</code> <code>RoleCreatedEvent</code> <code>.role</code> <code>server:role_updated</code> <code>RoleUpdatedEvent</code> <code>.role_id</code>, <code>.server_id</code> <code>server:role_deleted</code> <code>RoleDeletedEvent</code> <code>.role_id</code>, <code>.server_id</code> <code>user:presence_update</code> <code>PresenceUpdatedEvent</code> <code>.user_id</code>, <code>.status</code>, <code>.custom</code> <code>channel:typing</code> <code>TypingEvent</code> <code>.channel_id</code>, <code>.user_id</code> <p>Unknown events fall back to raw dicts \u2014 nothing breaks when Nerimity adds new events.</p>"},{"location":"api/events/#error-isolation","title":"Error isolation","text":"<p>Each handler runs independently. A crash in one handler does not affect others.</p>"},{"location":"api/permissions/","title":"Permissions","text":""},{"location":"api/permissions/#permission-flags","title":"Permission flags","text":"<pre><code>from nerimity_sdk import Permissions\n\nPermissions.ADMIN\nPermissions.SEND_MESSAGES\nPermissions.MANAGE_ROLES\nPermissions.KICK_MEMBERS\nPermissions.BAN_MEMBERS\nPermissions.MANAGE_CHANNELS\nPermissions.MANAGE_SERVER\n</code></pre>"},{"location":"api/permissions/#checking-permissions","title":"Checking permissions","text":"<pre><code>from nerimity_sdk import has_permission, resolve_permissions\n\n# True if member has the permission (admin bypasses all)\nif has_permission(member, server, Permissions.KICK_MEMBERS):\n    ...\n\n# Get combined permission bits for a member\nperms = resolve_permissions(member, server)\n</code></pre>"},{"location":"api/permissions/#role-hierarchy","title":"Role hierarchy","text":"<pre><code>from nerimity_sdk import role_position, can_target\n\n# Highest role order for a member\npos = role_position(member, server)\n\n# True if actor's highest role is above target's\nif can_target(actor_member, target_member, server):\n    await ctx.rest.kick_member(server_id, target_id)\n</code></pre>"},{"location":"api/permissions/#per-command-permissions","title":"Per-command permissions","text":"<pre><code>@bot.command(\"ban\", required_user_perms=[Permissions.BAN_MEMBERS])\nasync def ban(ctx):\n    ...  # only runs if user has BAN_MEMBERS\n</code></pre>"},{"location":"api/plugins/","title":"Plugins","text":"<p>Plugins let you split your bot into reloadable modules.</p>"},{"location":"api/plugins/#creating-a-plugin","title":"Creating a plugin","text":"<pre><code>from nerimity_sdk import PluginBase, listener\n\nclass ModerationPlugin(PluginBase):\n    name = \"moderation\"\n    description = \"Moderation commands\"\n\n    @listener(\"server:member_joined\")\n    async def on_join(self, event):\n        # self.bot is the Bot instance\n        await self.bot.rest.create_message(\n            event.member.server_id, f\"Welcome!\"\n        )\n\n    async def on_load(self):\n        print(f\"[{self.name}] Loaded\")\n\n    async def on_unload(self):\n        print(f\"[{self.name}] Unloaded\")\n\n    async def on_ready(self):\n        print(f\"[{self.name}] Bot is ready\")\n\n\nasync def setup(bot):\n    await bot.plugins.load(ModerationPlugin())\n</code></pre>"},{"location":"api/plugins/#loading","title":"Loading","text":"<pre><code># From an instance\nawait bot.plugins.load(ModerationPlugin())\n\n# From a file path\nawait bot.plugins.load_from_path(\"plugins/moderation.py\")\n</code></pre>"},{"location":"api/plugins/#hot-reload","title":"Hot reload","text":"<pre><code>await bot.plugins.reload(\"moderation\")\n</code></pre> <p>Calls <code>importlib.reload</code> on the module, re-instantiates the plugin class, and re-registers all commands and listeners.</p>"},{"location":"api/plugins/#unloading","title":"Unloading","text":"<pre><code>await bot.plugins.unload(\"moderation\")\n</code></pre>"},{"location":"api/plugins/#listing-loaded-plugins","title":"Listing loaded plugins","text":"<pre><code>print(bot.plugins.loaded)   # [\"moderation\", \"welcome\", ...]\n</code></pre>"},{"location":"api/plugins/#watch-mode","title":"Watch mode","text":"<p>With <code>watch=True</code>, plugins reload automatically when their <code>.py</code> file changes:</p> <pre><code>bot = Bot(token=\"...\", watch=True, watch_paths=[\"plugins\"])\n</code></pre>"},{"location":"api/scheduler/","title":"Scheduler","text":"<p>Run tasks on a cron schedule. Requires <code>pip install croniter</code>.</p>"},{"location":"api/scheduler/#usage","title":"Usage","text":"<pre><code>@bot.cron(\"0 9 * * 1\")   # Every Monday at 09:00 UTC\nasync def weekly_report():\n    await bot.rest.create_message(CHANNEL_ID, \"Weekly report!\")\n\n@bot.cron(\"*/30 * * * *\")  # Every 30 minutes\nasync def heartbeat():\n    print(\"Still alive\")\n</code></pre> <p>Jobs start automatically when the bot is ready and stop on shutdown.</p>"},{"location":"api/scheduler/#cron-syntax","title":"Cron syntax","text":"<pre><code>\u250c\u2500 minute (0-59)\n\u2502 \u250c\u2500 hour (0-23)\n\u2502 \u2502 \u250c\u2500 day of month (1-31)\n\u2502 \u2502 \u2502 \u250c\u2500 month (1-12)\n\u2502 \u2502 \u2502 \u2502 \u250c\u2500 day of week (0-6, Sun=0)\n\u2502 \u2502 \u2502 \u2502 \u2502\n* * * * *\n</code></pre> <p>Common examples:</p> Expression Meaning <code>0 9 * * *</code> Every day at 09:00 <code>0 9 * * 1</code> Every Monday at 09:00 <code>*/5 * * * *</code> Every 5 minutes <code>0 0 1 * *</code> First day of every month"},{"location":"api/slash/","title":"Slash Commands","text":"<p>Slash commands in nerimity-sdk are the same as prefix commands \u2014 <code>@bot.slash</code> is an alias for <code>@bot.command</code>.</p> <pre><code>@bot.command(\"ban\", description=\"Ban a user\")\nasync def ban(ctx):\n    ...\n</code></pre> <p>This registers <code>/ban</code> in Nerimity's slash menu and handles <code>!ban</code>. You don't need a separate slash handler.</p>"},{"location":"api/slash/#botslash-alias","title":"<code>@bot.slash</code> alias","text":"<p>If you prefer the explicit name:</p> <pre><code>@bot.slash(\"ban\", description=\"Ban a user\")\nasync def ban(ctx):\n    ...\n</code></pre> <p>Identical behaviour.</p>"},{"location":"api/slash/#hiding-from-the-slash-menu","title":"Hiding from the slash menu","text":"<p>Use <code>@bot.command_private</code> (or <code>@bot.slash_private</code>) to keep a command prefix-only:</p> <pre><code>@bot.command_private(\"debug\")\nasync def debug(ctx):\n    await ctx.reply(\"internal info\")\n</code></pre> <p>This never appears in the <code>/</code> menu.</p>"},{"location":"api/slash/#slashcontext-vs-context","title":"SlashContext vs Context","text":"<p>There is no separate <code>SlashContext</code> \u2014 slash and prefix invocations both receive the same <code>Context</code> object. See Context for the full reference.</p>"},{"location":"api/slash/#registration","title":"Registration","text":"<p>Public commands are automatically synced to Nerimity's API on bot ready. You'll see:</p> <pre><code>[Bot] Synced 3 command(s): ['ping', 'ban', 'kick']\n</code></pre> <p>in the logs when it works.</p>"},{"location":"api/storage/","title":"Storage","text":"<p>A simple async key-value store. All backends implement the same interface.</p>"},{"location":"api/storage/#interface","title":"Interface","text":"<pre><code>await bot.store.get(\"key\")           # \u2192 Any | None\nawait bot.store.set(\"key\", value)    # value can be any JSON-serializable type\nawait bot.store.delete(\"key\")\nawait bot.store.keys(\"guild:*\")      # fnmatch pattern \u2192 list[str]\n</code></pre>"},{"location":"api/storage/#backends","title":"Backends","text":""},{"location":"api/storage/#memorystore-default","title":"MemoryStore (default)","text":"<p>No persistence. Useful for testing.</p> <pre><code>from nerimity_sdk import MemoryStore\nbot = Bot(token=\"...\", store=MemoryStore())\n</code></pre>"},{"location":"api/storage/#jsonstore","title":"JsonStore","text":"<p>Persists to a JSON file. Zero extra dependencies.</p> <pre><code>from nerimity_sdk import JsonStore\nbot = Bot(token=\"...\", store=JsonStore(\"data.json\"))\n</code></pre>"},{"location":"api/storage/#sqlitestore","title":"SqliteStore","text":"<p>Async SQLite. Requires <code>pip install aiosqlite</code>.</p> <pre><code>from nerimity_sdk import SqliteStore\nbot = Bot(token=\"...\", store=SqliteStore(\"bot.db\"))\n</code></pre>"},{"location":"api/storage/#redisstore","title":"RedisStore","text":"<p>Async Redis. Requires <code>pip install redis</code>.</p> <pre><code>import redis.asyncio as aioredis\nfrom nerimity_sdk import RedisStore\n\nr = aioredis.from_url(\"redis://localhost\")\nbot = Bot(token=\"...\", store=RedisStore(r, prefix=\"mybot:\"))\n</code></pre>"},{"location":"api/storage/#common-patterns","title":"Common patterns","text":"<pre><code># Per-guild settings\nprefix = await bot.store.get(f\"guild:{ctx.server_id}:prefix\") or \"!\"\nawait bot.store.set(f\"guild:{ctx.server_id}:prefix\", \"?\")\n\n# User data\nawait bot.store.set(f\"user:{ctx.author.id}:xp\", 100)\n\n# List all guild prefixes\nkeys = await bot.store.keys(\"guild:*:prefix\")\n</code></pre>"},{"location":"api/utils/","title":"Utilities","text":""},{"location":"api/utils/#mentions","title":"Mentions","text":"<p>Nerimity uses <code>[@:user_id]</code> syntax for mentions.</p> <pre><code>from nerimity_sdk import mention, parse_mention_ids, resolve_mentions\n\n# Format a user ID as a mention\nmention(\"123456789\")   # \u2192 \"[@:123456789]\"\n\n# Extract all user IDs from a string\nparse_mention_ids(\"Hello [@:111] and [@:222]\")   # \u2192 [\"111\", \"222\"]\n\n# Resolve mentions to cached User objects\nusers = resolve_mentions(message.content, bot.cache)\n</code></pre>"},{"location":"api/utils/#ctxmentions","title":"<code>ctx.mentions</code>","text":"<p>The <code>Context</code> object exposes a <code>mentions</code> property that automatically resolves all <code>[@:id]</code> mentions in the triggering message:</p> <pre><code>@bot.command(\"ping_all\")\nasync def ping_all(ctx):\n    for user in ctx.mentions:\n        await ctx.reply(f\"Pinging {user.username}!\")\n</code></pre>"},{"location":"api/utils/#paginator","title":"Paginator","text":"<pre><code>from nerimity_sdk import Paginator\n\npages = [\"Page 1 content\", \"Page 2 content\", \"Page 3 content\"]\nawait Paginator(pages, timeout=60).send(ctx)\n</code></pre> <ul> <li>If a <code>ButtonRouter</code> is wired (it is by default in bot commands), navigation uses prev/next buttons with the given TTL.</li> <li>Otherwise falls back to text-based <code>next</code>/<code>prev</code>/<code>stop</code> prompts via <code>ctx.ask()</code>.</li> </ul>"},{"location":"api/utils/#messagebuilder","title":"MessageBuilder","text":"<pre><code>from nerimity_sdk import MessageBuilder\n\nawait (MessageBuilder()\n    .content(\"Hello!\")\n    .reply_to(\"message_id\")\n    .silent()\n    .send(ctx.rest, ctx.channel_id))\n</code></pre>"},{"location":"api/utils/#embed","title":"Embed","text":"<pre><code>from nerimity_sdk import Embed\n\nembed = (Embed()\n    .title(\"Server Info\")\n    .description(\"Some details here\")\n    .url(\"https://nerimity.com\")\n    .image(\"https://example.com/image.png\")\n    .color(0x5865F2))\n\nd = embed.to_dict()   # serialize to dict for API calls\n</code></pre>"},{"location":"guide/configuration/","title":"Configuration","text":"<p>All options are passed to the <code>Bot</code> constructor.</p> <pre><code>bot = Bot(\n    token=\"YOUR_TOKEN\",          # required\n    prefix=\"!\",                  # default command prefix\n    prefix_store=None,           # pluggable per-guild prefix backend\n    cache_size=1000,             # max objects per cache type\n    cache_ttl=0,                 # TTL in seconds (0 = no expiry)\n    cache_invalidate_on_disconnect=True,  # wipe cache on disconnect\n    debug=False,                 # pretty-print all gateway payloads\n    watch=False,                 # auto-reload plugins on file save\n    watch_paths=[\"plugins\"],     # directories to watch\n    store=None,                  # persistent storage backend\n    shard_id=0,                  # shard index (future use)\n    shard_count=1,               # total shards (future use)\n)\n</code></pre>"},{"location":"guide/configuration/#per-guild-prefix","title":"Per-guild prefix","text":"<pre><code>from nerimity_sdk import MemoryPrefixStore\n\nbot = Bot(token=\"...\", prefix=\"!\")\n\n# At runtime:\nawait bot.prefix_resolver.set(\"guild_id\", \"?\")\nawait bot.prefix_resolver.reset(\"guild_id\")   # back to default\n</code></pre>"},{"location":"guide/configuration/#storage-backends","title":"Storage backends","text":"<pre><code>from nerimity_sdk import JsonStore, SqliteStore, RedisStore, MemoryStore\n\nbot = Bot(token=\"...\", store=JsonStore(\"data.json\"))   # default\nbot = Bot(token=\"...\", store=SqliteStore(\"bot.db\"))    # pip install aiosqlite\nbot = Bot(token=\"...\", store=RedisStore(redis_client)) # pip install redis\nbot = Bot(token=\"...\", store=MemoryStore())            # no persistence\n</code></pre>"},{"location":"guide/configuration/#debug-mode","title":"Debug mode","text":"<pre><code>bot = Bot(token=\"...\", debug=True)\n</code></pre> <p>Logs every raw gateway payload with pretty JSON formatting. Useful when reverse-engineering undocumented events.</p>"},{"location":"guide/configuration/#watch-mode","title":"Watch mode","text":"<pre><code>bot = Bot(token=\"...\", watch=True, watch_paths=[\"plugins\"])\n</code></pre> <p>Requires <code>pip install watchfiles</code>. Automatically reloads plugins when their <code>.py</code> file changes on disk.</p>"},{"location":"guide/installation/","title":"Installation","text":""},{"location":"guide/installation/#requirements","title":"Requirements","text":"<ul> <li>Python 3.10+</li> <li>A Nerimity bot token</li> </ul> <p>Getting a token:</p> <ol> <li>Go to nerimity.com/app/settings/developer/applications</li> <li>Create a new application</li> <li>Add a Bot user to it</li> <li>Copy the token \u2014 that's your <code>NERIMITY_TOKEN</code></li> </ol>"},{"location":"guide/installation/#install","title":"Install","text":"<pre><code>pip install nerimity-sdk\n</code></pre>"},{"location":"guide/installation/#optional-extras","title":"Optional extras","text":"<pre><code># Redis cache / storage backend\npip install \"nerimity-sdk[redis]\"\n\n# SQLite storage backend\npip install \"nerimity-sdk[sqlite]\"\n\n# Cron scheduler\npip install \"nerimity-sdk[cron]\"\n\n# Dev watch mode (auto-reload plugins on file save)\npip install \"nerimity-sdk[watch]\"\n\n# Everything\npip install \"nerimity-sdk[redis,sqlite,cron,watch]\"\n</code></pre>"},{"location":"guide/installation/#scaffold-a-project","title":"Scaffold a project","text":"<pre><code>nerimity create my-bot\ncd my-bot\n</code></pre> <p>This creates:</p> <pre><code>my-bot/\n\u251c\u2500\u2500 bot.py              # Main bot file\n\u251c\u2500\u2500 plugins/\n\u2502   \u2514\u2500\u2500 greeter.py      # Example plugin\n\u251c\u2500\u2500 .env.example        # Token template\n\u251c\u2500\u2500 .gitignore\n\u2514\u2500\u2500 README.md\n</code></pre> <p>Set your token:</p> <pre><code>cp .env.example .env\n# Edit .env and set NERIMITY_TOKEN=your_token_here\n</code></pre> <p>Then run:</p> <pre><code>python bot.py\n</code></pre>"},{"location":"guide/quickstart/","title":"Quick Start","text":"<p>This guide walks you from zero to a working bot in 5 minutes.</p>"},{"location":"guide/quickstart/#1-get-a-token","title":"1. Get a token","text":"<p>Go to nerimity.com/app/settings/developer/applications.</p> <ol> <li>Click New Application</li> <li>Go to the Bot tab and click Add Bot</li> <li>Copy the token</li> </ol>"},{"location":"guide/quickstart/#2-create-a-project","title":"2. Create a project","text":"<pre><code>nerimity create my-bot\ncd my-bot\ncp .env.example .env\n</code></pre> <p>Open <code>.env</code> and paste your token:</p> <pre><code>NERIMITY_TOKEN=paste_your_token_here\n</code></pre>"},{"location":"guide/quickstart/#3-run-it","title":"3. Run it","text":"<pre><code>python bot.py\n</code></pre> <p>You should see <code>Logged in as YourBot#0000</code> in the terminal.</p>"},{"location":"guide/quickstart/#4-add-your-first-command","title":"4. Add your first command","text":"<p>Open <code>bot.py</code>. Add this anywhere before <code>bot.run()</code>:</p> <pre><code>@bot.command(\"hello\", description=\"Say hello back\")\nasync def hello(ctx):\n    await ctx.reply(f\"Hello, {ctx.author.username}!\")\n</code></pre> <p>Save the file and restart. Type <code>!hello</code> in any channel the bot can see.</p>"},{"location":"guide/quickstart/#5-read-arguments","title":"5. Read arguments","text":"<pre><code>@bot.command(\"say\", description=\"Repeat something\")\nasync def say(ctx):\n    # ctx.args is a list of words the user typed after !say\n    if not ctx.args:\n        return await ctx.reply(\"Usage: !say &lt;message&gt;\")\n    await ctx.reply(\" \".join(ctx.args))\n</code></pre> <p>Type <code>!say hello world</code> \u2192 bot replies <code>hello world</code>.</p>"},{"location":"guide/quickstart/#6-use-typed-converters","title":"6. Use typed converters","text":"<p>Instead of parsing strings yourself, declare what types you expect:</p> <pre><code>from nerimity_sdk import Int\n\n@bot.command(\"double\", description=\"Double a number\", args=[Int])\nasync def double(ctx):\n    n = ctx.args[0]   # already an int\n    await ctx.reply(str(n * 2))\n</code></pre> <p>If the user types <code>!double abc</code>, they get a friendly error automatically.</p>"},{"location":"guide/quickstart/#7-handle-errors","title":"7. Handle errors","text":"<p>Add this once and all command errors show a message instead of silently failing:</p> <pre><code>@bot.on_command_error\nasync def on_error(ctx, error):\n    await ctx.reply(f\"\u274c {error}\")\n</code></pre>"},{"location":"guide/quickstart/#8-slash-commands-work-automatically","title":"8. Slash commands work automatically","text":"<p>Every <code>@bot.command</code> is already a slash command \u2014 it shows up in Nerimity's <code>/</code> menu automatically. No extra code needed.</p> <pre><code>@bot.command(\"ping\", description=\"Check if the bot is alive\")\nasync def ping(ctx):\n    await ctx.reply(\"Pong!\")\n# Users can now type !ping OR /ping\n</code></pre> <p>To keep a command prefix-only (hidden from the <code>/</code> menu):</p> <pre><code>@bot.command_private(\"debug\")\nasync def debug(ctx):\n    await ctx.reply(\"secret\")\n</code></pre>"},{"location":"guide/quickstart/#9-save-data-between-restarts","title":"9. Save data between restarts","text":"<pre><code>from nerimity_sdk import JsonStore\n\nbot = Bot(token=os.environ[\"NERIMITY_TOKEN\"], prefix=\"!\", store=JsonStore(\"data.json\"))\n\n@bot.command(\"remember\", description=\"Remember a value\")\nasync def remember(ctx):\n    if not ctx.args:\n        return await ctx.reply(\"Usage: !remember &lt;value&gt;\")\n    await bot.store.set(f\"user:{ctx.author.id}:note\", ctx.args[0])\n    await ctx.reply(f\"Remembered: {ctx.args[0]}\")\n\n@bot.command(\"recall\", description=\"Recall your saved value\")\nasync def recall(ctx):\n    note = await bot.store.get(f\"user:{ctx.author.id}:note\")\n    await ctx.reply(note or \"Nothing saved yet.\")\n</code></pre>"},{"location":"guide/quickstart/#whats-next","title":"What's next?","text":"<ul> <li>Commands in depth \u2014 converters, middleware, permissions, help generator</li> <li>Slash commands \u2014 <code>/</code> commands with argument types</li> <li>Buttons \u2014 interactive button handlers</li> <li>Plugins \u2014 split your bot into reloadable modules</li> <li>Example Bot \u2014 a complete bot showing everything at once</li> </ul>"}]}